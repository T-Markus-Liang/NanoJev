interface:
  display_name: "NanoJev Local Decider"
  short_description: "Use local NanoJev for structured decisions"
  default_prompt: "Use $nanojev-local-decider for a local structured decision and record privacy-preserving usage telemetry."
policy:
  allow_implicit_invocation: true
